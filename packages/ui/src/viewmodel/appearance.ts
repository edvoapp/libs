import { PropertyNode } from './base';

/**
 * PropertyNode to prevent X from chaging the appearance of Y's card to portal to stop Y from unwittingly oversharing
 * TODO: We need to take special care for any switches that differentiate different levels of recursion
 */
export class Appearance extends PropertyNode {}
