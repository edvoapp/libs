// CURRENTLY UNUNSED
// import { Model } from '@edvoapp/common';
// import { useObserveValue } from '@edvoapp/util';

// export function MarginNote({ vertex }: { vertex: Model.Vertex }) {
//   const bodyText = useObserveValue(() => vertex.bodyTextObs(), [vertex]);
//   return <div className="margin-note">{bodyText?.substring(0, 500)}</div>;
// }
