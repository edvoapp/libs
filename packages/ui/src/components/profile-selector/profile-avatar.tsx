import { PersonIcon } from '../../assets';

export const ProfileAvatar = () => (
  <div className="profiles__list__item__profile-avatar">
    <div className="img-wrapper">
      <PersonIcon />
    </div>
  </div>
);
